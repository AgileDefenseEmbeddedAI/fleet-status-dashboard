import { AssetType, AssetStatus } from '../types';

export const ALL_TYPES: AssetType[] = ['ship', 'drone', 'airplane'];
export const ALL_STATUSES: AssetStatus[] = ['active', 'idle', 'maintenance', 'offline'];
